@model SD_340_W22SD_Final_Project_Group6.Models.ViewModel.ProjectAndTicketAndUsersViewModel


@{
	ViewData["Title"] = "Create";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>Ticket for @Model.curProject.ProjectName</h4>
<hr />
<div class="row">
	<div>
		<form asp-action="Create" method="post">
			<div asp-validation-summary="ModelOnly" class="text-danger"></div>


			<div class="form-group">
				<label asp-for="@Model.curTicket.Title" class="control-label"></label>
				<input asp-for="@Model.curTicket.Title" class="form-control" />
				<span asp-validation-for="@Model.curTicket.Title" class="text-danger"></span>
			</div>
			<div class="form-group">
				<label asp-for="@Model.curTicket.Body" class="control-label"></label>
				<textarea asp-for="@Model.curTicket.Body" id="mytextarea" required="required">Enter Text Here</textarea>
				<span asp-validation-for="@Model.curTicket.Body" class="text-danger"></span>
			</div>
			<div class="row">
				<div class="col-2" style="gap: 1rem; display:flex; flex-direction:column">
					<div class="form-group">
						<label asp-for="@Model.curTicket.RequiredHours" class="control-label"></label>
						<input asp-for="@Model.curTicket.RequiredHours" class="form-control" />
						<span asp-validation-for="@Model.curTicket.RequiredHours" class="text-danger"></span>
					</div>
					<div class="form-group">
						<label asp-for="@Model.curTicket.Project" class="control-label"></label>
						<input type="hidden" value="@Model.curProject.Id" name="projId" />
						<p style="font-weight: bold; display: inline">@Model.curProject.ProjectName</p>
						<span asp-validation-for="@Model.curTicket.Project" class="text-danger"></span>
					</div>


				</div>
				<div class="col-8" style="gap: 1rem; display:flex; flex-direction:column">
					<div class="form-group">
						<label asp-for="@Model.curTicket.TicketPriority" class="control-label"></label>
						<select asp-for="@Model.curTicket.TicketPriority" asp-items="Html.GetEnumSelectList<Ticket.Priority>()"></select>
						<span asp-validation-for="@Model.curTicket.TicketPriority" class="text-danger"></span>
					</div>
					<div class="form-group">
						<label asp-for="@Model.curTicket.Owner" class="control-label"></label>
						<select name="userId" asp-for="@Model.curTicket.Owner" asp-items=@Model.curUsers></select>
						<span asp-validation-for="@Model.curTicket.Owner" class="text-danger"></span>
					</div>
				</div>
			</div>

			<div class="form-group">
				<input type="submit" value="Create" class="btn" style="background-color: #E4BCB9" />
			</div>


		</form>
	</div>
</div>

<div>
	<a asp-action="Index" asp-controller="Projects">Back to List</a>
</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
	}
