@model SD_340_W22SD_Final_Project_Group6.Models.ViewModel.ProjectAndTicketAndUsersViewModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Ticket</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="@Model.curTicket.Id" />
            <div class="form-group">
                <label asp-for="@Model.curTicket.Title" class="control-label"></label>
                <input asp-for="@Model.curTicket.Title" class="form-control" />
                <span asp-validation-for="@Model.curTicket.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.curTicket.Body" class="control-label"></label>
                <input asp-for="@Model.curTicket.Body" class="form-control" />
                <span asp-validation-for="@Model.curTicket.Body" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.curTicket.RequiredHours" class="control-label"></label>
                <input asp-for="@Model.curTicket.RequiredHours" class="form-control" />
                <span asp-validation-for="@Model.curTicket.RequiredHours" class="text-danger"></span>
            </div>
            <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.curTicket.Owner)
            </th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.curTicket.Owner</td>
        </tr>
    </tbody>
</table>
                <div class="form-group">
                    <input type="hidden" value=@Model.curTicket.Id name="ticketId" />
                     <label asp-for="@Model.curTicket.Owner" class="control-label">Select Users:</label>
                <select name="userId" asp-for="@Model.curTicket.Owner" asp-items=@Model.curUsers></select>
                <span asp-validation-for="@Model.curTicket.Owner" class="text-danger"></span>
                </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-controller="Projects">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
